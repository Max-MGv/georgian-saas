dashboard/node_modules
dashboard/.next
dashboard/.env.local
dashboard/.env*.local

# Main app
saas/node_modules
saas/.next
saas/.env
saas/.env*.local

# Credentials — never commit
credentials.txt

.vercel
